openapi: 3.0.3
info: { title: Mechanic Shop API, version: "1.0.0" }
paths:
  /health:
    get:
      summary: Health check
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
